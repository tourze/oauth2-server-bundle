services:
  # default configuration for services in *this* file
  _defaults:
    autowire: true      # Automatically injects dependencies in your services.
    autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

  Tourze\OAuth2ServerBundle\Controller\:
    resource: '../../Controller/'
  Tourze\OAuth2ServerBundle\DataFixtures\:
    resource: '../../DataFixtures/'
  Tourze\OAuth2ServerBundle\Repository\:
    resource: '../../Repository/'
  Tourze\OAuth2ServerBundle\Service\:
    resource: '../../Service/'

  # Explicit service definitions for testing
  tourze_oauth2_server.service.oauth2_client_service:
    alias: Tourze\OAuth2ServerBundle\Service\OAuth2ClientService
    public: true
